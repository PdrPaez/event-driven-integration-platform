import React,{useEffect,useState} from 'react'; import {createRoot} from 'react-dom/client'; import {ReactFlow,Background,Controls,MiniMap,Node,Edge} from '@xyflow/react'; import '@xyflow/react/dist/style.css'; import './style.css';
const nodes:Node[]=[{id:'source',position:{x:0,y:120},data:{label:'Domain command / Webhook'},type:'input'},{id:'tx',position:{x:230,y:120},data:{label:'PostgreSQL transaction'}},{id:'outbox',position:{x:470,y:120},data:{label:'Transactional outbox'}},{id:'broker',position:{x:700,y:120},data:{label:'RabbitMQ topic exchange'}},{id:'crm',position:{x:950,y:20},data:{label:'CRM → Inbox → destination'}},{id:'notify',position:{x:950,y:120},data:{label:'Notifications → Inbox'}},{id:'analytics',position:{x:950,y:220},data:{label:'Analytics → Inbox'}},{id:'retry',position:{x:700,y:300},data:{label:'Retry queues · 250/500ms'}},{id:'dlq',position:{x:470,y:300},data:{label:'Dead letter → durable replay'}}]; const edges:Edge[]=([{source:'source',target:'tx'},{source:'tx',target:'outbox'},{source:'outbox',target:'broker'},{source:'broker',target:'crm'},{source:'broker',target:'notify'},{source:'broker',target:'analytics'},{source:'crm',target:'retry'},{source:'retry',target:'broker'},{source:'retry',target:'dlq'}] as Edge[]).map((e,i)=>({...e,id:String(i),animated:true}));
function App(){const [events,setEvents]=useState<any[]>([]); const [status,setStatus]=useState('System Ready'); useEffect(()=>{fetch('/api/events').then(r=>r.ok?r.json():[]).then(setEvents).catch(()=>setStatus('API offline — start FastAPI'))},[]); return <main><header><div><span className="eyebrow">RELIABILITY EXPLORER</span><h1>Event-Driven Integration Platform</h1></div><span className="status">● {status}</span></header><section className="shell"><aside><h2>Scenarios</h2>{['Happy Path Fan-out','Transient Retry','Retry Exhaustion → DLQ','Dead-letter Replay','Duplicate → Inbox Skip','Schema v1 → v2'].map(x=><button key={x}>{x}</button>)}<div className="stats"><b>{events.length}</b><span>persisted events</span></div></aside><div className="canvas"><ReactFlow nodes={nodes} edges={edges} fitView><Background color="#263348"/><Controls/><MiniMap/></ReactFlow></div></section><section className="lower"><div><h2>Recent events</h2>{events.slice(0,5).map(e=><article key={e.event_id}><b>{e.event_type}</b><code>{e.event_id.slice(0,8)}…</code><span>schema v{e.schema_version}</span></article>)}{!events.length&&<p>Create an order to watch a real event enter the outbox.</p>}</div><div className="principle"><h2>Delivery contract</h2><p><strong>At-least-once delivery with idempotent processing.</strong></p><p>Publisher confirms and consumer ACKs are intentionally separate from database transactions. Duplicate publication is expected in the crash window; inbox and connector idempotency make the side effect safe.</p></div></section></main>} createRoot(document.getElementById('root')!).render(<App/>);
