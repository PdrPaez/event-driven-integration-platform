name: ci
on: [push, pull_request]
jobs:
  backend:
    runs-on: ubuntu-latest
    services:
      postgres: {image: postgres:16, env: {POSTGRES_PASSWORD: integration_dev, POSTGRES_USER: integration, POSTGRES_DB: integration}, ports: ['5432:5432'], options: --health-cmd pg_isready --health-interval 10s --health-timeout 5s --health-retries 5}
      rabbitmq: {image: rabbitmq:3.13, ports: ['5672:5672']}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.12'}
      - run: pip install -e 'backend[dev]'
      - run: ruff check backend
      - run: pytest backend
  frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: {node-version: '20'}
      - run: npm install
        working-directory: frontend
      - run: npm run lint && npm run build
        working-directory: frontend
